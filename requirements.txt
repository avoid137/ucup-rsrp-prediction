# 基站 RSRP 预测流水线依赖
# 开发环境：Python 3.10 / Windows（CPU 训练）
# 整理版另在 Python 3.11 / 3.13 + xgboost 3.4 上跑过合成数据全流程

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3
matplotlib>=3.7

# 点云处理与泊松重建（仅步骤 1、2 需要）
# Windows 上建议用 pip 直接装：pip install open3d
open3d>=0.17
